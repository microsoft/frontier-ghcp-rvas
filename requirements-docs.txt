mkdocs==1.6.1
mkdocs-material==9.5.49
pymdown-extensions==10.14
# Pinned: pymdown-extensions 10.14 passes filename=None for untitled code
# blocks, which Pygments 2.19+ no longer coerces to "" (crashes html.escape).
# 2.18.0 is the last release that handles it. Revisit when bumping the theme.
pygments==2.18.0

