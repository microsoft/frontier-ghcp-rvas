# Basher — History

## Core Context

- Project: GitHub Copilot Enterprise Hackathon challenge repo
- Owner: Marco Olivo
- Devcontainer configs under .devcontainer/ -- one per challenge (challenge-0 through challenge-5, plus bonus)
- Clean-start scripts (scripts/clean-start.sh, scripts/clean-start.ps1) wipe .github/ and detach remote
- Setup scripts (scripts/setup-challenge.sh, scripts/setup-challenge.ps1) for challenge-specific init
- Codespaces is primary delivery -- containers must boot fast and clean
- Tooling per challenge: Node.js, Python 3.11, .NET 8.0, Terraform, Docker-in-Docker, Jupyter, etc.
- VS Code workspace settings in .vscode/

## Learnings

- Added step 5 to both clean-start scripts (`.sh` and `.ps1`): after wiping `.github/` contents and removing the remote, the scripts now run `git add -A` and `git commit` to give participants a committed baseline. Previously the cleanup left uncommitted changes floating in the working tree, which is messy for people unfamiliar with git. The commit message is "Clean start: reset for hackathon". Updated header comments and closing output to match.

📌 Team update (2026-04-28): Docs publishing is expected to use a dedicated GitHub Pages workflow with the same `mkdocs build --strict` command used locally. Treat MkDocs validation as part of the deployment contract, not an optional local check. -- decided by Scribe

- (2026-05-14) `.gitattributes` had zero eol rules -- only Squad merge-driver entries. Windows clones with `core.autocrlf=true` were injecting CRLF into all `.sh` files, breaking shebangs (`bad interpreter: /usr/bin/env bash^M`). Fixed by prepending `* text=auto eol=lf`, `*.sh text eol=lf`, and `*.ps1 text eol=crlf` before the Squad section. Ran `git add --renormalize .` to re-normalize already-checked-out files. Verified with `git check-attr` (all three scripts/\*.sh now show `eol: lf`) and `git ls-files --eol` (all show `i/lf w/lf`). Affected files confirmed clean: `scripts/setup-challenge.sh`, `scripts/clean-start.sh`, `scripts/_clean-common.sh`, `challenges/challenge-14-pipeline-factory/scripts/deploy.sh`, `.devcontainer/challenge-19-wcf-banking/setup-wcf.sh`, `.devcontainer/challenge-11-mumps-banking/setup-yottadb.sh`, `.devcontainer/challenge-18-cobol-banking/setup-gnucobol.sh`, `.squad/templates/skills/distributed-mesh/sync-mesh.sh`, `.copilot/skills/distributed-mesh/sync-mesh.sh`.

- (2026-06-01) GitHub Pages docs theme upgrade. Swapped `mkdocs.yml` `theme: name: mkdocs` for **Material for MkDocs** (`mkdocs-material==9.5.49`). Enabled features: navigation.tabs/sections/top/indexes/footer, toc.follow, search.suggest/highlight, content.code.copy/annotate. Added light/dark `palette` toggle (default + slate, indigo primary). Added `markdown_extensions`: admonition, attr_list, md_in_html, tables, footnotes, toc(permalink), pymdownx.highlight (Material's official config: anchor_linenums + line_spans:__span + pygments_lang_class), inlinehilite, snippets, superfences, tabbed, details, emoji. Kept existing `nav`, `site_name`, `site_description`, `repo_url` untouched.
- **Version pins (requirements-docs.txt):** `mkdocs-material==9.5.49`, `pymdown-extensions==10.14`, and critically `pygments==2.18.0`. pymdown-extensions 10.14 passes `filename=None` for untitled code blocks; Pygments 2.19+ stopped coercing None to "" and crashes in `html.escape` (`AttributeError: 'NoneType' object has no attribute 'replace'`). This crash is NOT Python-version-specific -- reproduced on local 3.13/3.14 AND would hit CI's Python 3.11. Pinning `pygments==2.18.0` is mandatory for the deploy to pass, not just a local convenience. Revisit the pin when bumping mkdocs-material/pymdown-extensions.
- **--strict result:** PASS (EXIT=0) from a clean venv install of the pinned requirements. No broken internal links or nav errors. Build ~2.3s. Note: many `tracks/.../phase-*/*.md` and `stage-*.md` files exist in `docs/` but are not in `nav` -- mkdocs reports these as INFO (not WARNING), so `--strict` does not fail on them. Left as-is.
- **Deploy workflow:** `.github/workflows/deploy-docs.yml` left intact -- already correct (installs `requirements-docs.txt`, `mkdocs build --strict`, upload-pages-artifact@v3 + deploy-pages@v4, Python 3.11). Material needs no system deps (pure pip), so no workflow changes required.
- **Manual caveat (cannot be done from code):** repo owner must set GitHub → Settings → Pages → Source = "GitHub Actions" once in the UI for the deploy job to publish.

- (2026-06-12) Added scaffold support for a standalone `challenge-21-azure-terraform` slice: new starter files live under `challenges/challenge-21-azure-terraform/terraform/azure`, and the dedicated devcontainer stays lean with Terraform + Azure CLI + GitHub CLI only. Also hardened `scripts/setup-challenge.sh` and `scripts/setup-challenge.ps1` so if a devcontainer ships before its track guide lands in `tracks/`, the setup flow preserves shared track docs instead of deleting them and writing a broken README link.

- (2026-06-12) Challenge 21 now has one participant Terraform root: `challenges/challenge-21-azure-terraform/terraform/azure`. Validate that root with `terraform fmt`, `terraform init`, and `terraform validate`; root-level duplicate `.tf` files under `terraform/` should not come back.

- (2026-06-15) Enriched Challenge 21's Azure Terraform scaffold so it is no longer a straight file-generation exercise. The root now has validation around approved Azure regions, constrained naming, tags, storage replication, backend access choices, retention settings, and log retention. The app and network modules also validate key inputs. The challenge docs now include plan review, platform notes, deployment notes, and drift incident evidence that require judgment instead of ready-made answers. Keep validation anchored at `challenges/challenge-21-azure-terraform/terraform/azure`; workflow paths still assume the participant repo root is `challenges/challenge-21-azure-terraform` after setup.

- (2026-06-23) Migrated GitHub Pages deployment from MkDocs (Python build, mkdocs.yml, deploy-docs.yml workflow) to bespoke Node.js site. New workflow: `.github/workflows/deploy-site.yml` triggers on push to main + workflow_dispatch, runs `node web/build.js`, uploads from web/, deploys to GitHub Pages with proper permissions and concurrency control. Deleted old infrastructure: `mkdocs.yml`, `requirements-docs.txt`, `site/` build output, `scripts/generate-challenge-catalog.py`, old curator pages/assets in docs/, nested docs/docs/ directory. Kept consumed pages: `docs/copilot-guide.md`, `docs/prompt-engineering.md`, `docs/mcp-servers.md`, `docs/images/`, root `FACILITATOR_GUIDE.md`, `TROUBLESHOOTING.md`, `tracks/getting-started.md`. Temporary stub cleanup by linus-foundation accidentally deleted Rusty's challenge-0/challenge-1 meta.yml and learning-paths.json; coordinator reconstructed them. Build validated: 22 challenges, 5 categories, 8 learning paths. Site ready for Azure-backed GitHub Pages deployment. Note: Manual step required once: GitHub → Settings → Pages → Source = "GitHub Actions" in the UI.

- (2026-06-23) Added BYOC kit (byoc/) and OUTCOMES.md to the repository as new top-level facilitator-facing reference materials (WS3 and WS2 of the outcome-driven reframe). Verified that scripts/clean-start.sh, scripts/setup-challenge.sh (and PowerShell equivalents) do not explicitly remove these files -- they are preserved by design. Added explicit comments to _clean-common.sh/.ps1, setup-challenge.sh/.ps1 to document that byoc/ and OUTCOMES.md are INTENTIONALLY PRESERVED as facilitator reference, not removed to keep participant workspaces minimal. Rationale: both materials are small (~65 KB combined), non-essential to participant work, but valuable for facilitator context. No build dependencies (web/build.js does not read byoc/). Behavior verified: bash -n syntax OK on all scripts, devcontainer post-create calls setup-challenge.sh unchanged, deploy-site.yml unaffected. Decision documented in .squad/decisions.md.

- (2026-06-23) **Team convention: Outcomes facet + BYOC kit (Scribe orchestration)** -- All 22 challenges now carry `outcomes` metadata (Danny's taxonomy: modernize-legacy, ship-features, raise-quality, automate-delivery, platform-foundation, build-ai). The hackathon narrative is outcome-driven (Rusty), website surfaces outcomes via badges/filtering (Linus), facilitators have BYOC kit + templates for running hackathons on customer codebases (Rusty + Linus), tooling preserves BYOC references in participant workspaces (this work), and QA verified consistency across all files (Livingston). Future infrastructure work must maintain outcomes data flow (meta.yml -> build.js -> platform.json) and preserve BYOC/OUTCOMES.md in published artifacts.

